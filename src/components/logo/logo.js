import './logo.css'

const Logo = (props) => {
    return (
        <div {...props} >
            <span className="logo-cross">cross</span>
            <span className="logo-life">life</span>
        </div>
    )
}

export default Logo